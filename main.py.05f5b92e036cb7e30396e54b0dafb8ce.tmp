import sys


def read_and_preprocess(name):
    X, y = data_preprocessing.read_and_fix(name)
    pipe = Pipeline(steps=[('imputation', data_preprocessing.imputation()), ('normalization', data_preprocessing.variance())])
    pipe.fit(X, y)
    X = pipe.transform(X)
    data_preprocessing.to_csv(X, y, name)


def main(database_name):
    
    

if __name__ == "__main__":
    database_name= sys.argv[1]
    main(database_name)